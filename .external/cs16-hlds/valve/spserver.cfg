// This config is executed whenever a single player game is launched.

// we should be pausable by default
pausable 1

// maximum client movement speed 
sv_maxspeed 320

// allow autoaim by default (disabled in server.cfg and listenserver.cfg)
sv_allow_autoaim 1

// allow shaders
sv_allow_shaders 1
