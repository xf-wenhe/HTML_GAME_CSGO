# Dust2 单人模式试玩修复目标

## 置顶修复目标

1. [P0] 修复桌面端战斗焦点与开火链路
   - 现象: 进入 Dust2 单人模式后 `pointerLockElement` 为空，锁定鼠标提示处于隐藏状态；在画布中心连续左键点击 5 次，Glock-18 弹匣仍为 `20`，没有开火消耗。
   - 目标: 进入战斗后必须明确引导并完成 Pointer Lock；获得焦点后左键点击应能稳定射击、消耗弹药并触发射击反馈。
   - 相关模块: `client/src/main.ts`, `client/src/game/InputManager.ts`, `client/src/game/PlayerController.ts`, `client/src/ui/HUD.ts`

2. [P0] 修复投掷物切换导致的武器/HUD 状态错乱
   - 现象: 开局显示 `4 高爆雷 x1`；按 `4` 后 HUD 变成 `4 闪光弹 x2`，但当前武器仍显示 `战术刀`，弹药显示为 `-- 闪光弹 x2`。
   - 目标: 按 `4` 只应选择玩家实际拥有的当前投掷物；不得把高爆雷错误替换成闪光弹，也不得让当前武器和弹药栏互相矛盾。
   - 相关模块: `client/src/game/WeaponManager.ts`, `client/src/game/GrenadeSystem.ts`, `client/src/ui/HUD.ts`, `client/src/main.ts`

3. [P1] 明确冻结时间和购买窗口
   - 现象: 重新开局后前 5 秒只显示 `00:05` 到 `00:01`，HUD 未显示 `FREEZE`/冻结状态；购买菜单默认隐藏。进入 LIVE 后按 `B` 会打开购买菜单，但只提示“只能在冻结购买时间购买”，遮挡战斗视野。
   - 目标: 冻结时间应有明确状态标签、倒计时和购买提示；LIVE 阶段按 `B` 时应给出轻量提示或短 toast，避免打开不可操作的大菜单遮挡画面。
   - 相关模块: `client/src/game/Cs16BotMatch.ts`, `client/src/ui/HUD.ts`, `client/src/main.ts`

4. [P1] 修复回合计数和击杀/回合反馈
   - 现象: 完整观察一轮后 CT 比分从 `0` 变成 `1`，但 HUD 仍显示 `第 0 回合`；击杀提示区域一直为空。
   - 目标: 每次回合结算后回合数应递增，比分变化应配套显示击杀信息或回合结果提示。
   - 相关模块: `client/src/game/Cs16BotMatch.ts`, `client/src/ui/HUD.ts`, `client/src/ui/KillFeed.ts`, `client/src/ui/Scoreboard.ts`

5. [P1] 修复本地 dev server 端口冲突体验
   - 现象: `npm run dev` 时 Vite 因 `5173` 被占用自动切到 `http://localhost:5174/`，但后端在 `3000` 端口占用时直接抛出 `EADDRINUSE` 并退出。
   - 目标: 后端端口占用时应给出可执行提示，或支持 `PORT` 配置/自动换端口；单人模式可继续，但 dev 命令不应留下误导性的半启动状态。
   - 相关模块: `server/index.ts`, `package.json`

## 试玩环境

- 日期: 2026-06-18
- 方式: 使用 Chrome 插件实际打开本地页面试玩 Dust2 单人模式。
- 启动命令: `npm run dev`
- 实际访问地址: `http://localhost:5174/`
- 环境备注:
  - Vite 默认端口 `5173` 被占用，自动切换到 `5174`。
  - 后端 `3000` 端口被占用，`server/index.ts` 启动失败并输出 `EADDRINUSE`。
  - 单人 Dust2 仍可进入，HUD 显示 `Dust2 Bot Match`、`5/5 BOT`。
  - 试玩过程未观察到浏览器 console error。
- 覆盖范围:
  - 主菜单选择 Dust2 + 单人模式。
  - 开局冻结倒计时、LIVE 阶段、完整回合流转。
  - 购买菜单、键盘切枪、左键开火、换弹、无线电菜单、计分板触发。

## 详细问题清单

### [P0] 桌面端左键开火无效

- 现象: Glock-18 当前弹匣为 `20`；点击画布中心多次后仍为 `20`，没有弹药消耗。
- 期望: 左键点击应射击并让弹药从 `20` 递减，同时有声音、枪口/后坐力/命中反馈。
- 复现:
  1. 启动 `npm run dev`。
  2. 进入 `Dust2` -> `单人任务闯关`。
  3. LIVE 阶段确认当前武器为 `Glock-18`。
  4. 点击画布中心多次。
  5. 观察弹药仍为 `20`。
- 建议修复目标: 进入战斗时显示并完成鼠标锁定；锁定失败时不要隐藏提示；输入管理应在焦点可用后才允许战斗开始。

### [P0] 投掷物槽位和当前武器状态错乱

- 现象: 按 `3` 后当前武器为 `战术刀`；按 `4` 后投掷物槽位从 `高爆雷 x1` 变成 `闪光弹 x2`，但当前武器仍为 `战术刀`。
- 期望: 按 `4` 后当前武器应显示实际选中的投掷物，槽位数量不应凭空变化。
- 复现:
  1. 进入 Dust2 单人模式 LIVE 阶段。
  2. 观察初始槽位为 `1 -- / 2 Glock-18 / 3 战术刀 / 4 高爆雷 x1`。
  3. 按 `3`，再按 `4`。
  4. 观察 HUD 显示 `4 闪光弹 x2`，当前武器仍为 `战术刀`。
- 建议修复目标: 统一武器管理器和投掷物管理器的当前选择状态；HUD 只渲染真实 inventory，不用默认 grenade 配置覆盖当前局状态。

### [P1] 冻结时间缺少明确状态

- 现象: 新开局前 5 秒 timer 显示 `00:05`、`00:04`、`00:03`、`00:02`、`00:01`，但 phase 文案为空；随后直接显示 `LIVE` 和 `01:55`。
- 期望: 冻结时间应明确显示 `FREEZE` 或“冻结购买时间”，并提示玩家此时可购买、不可移动/射击。
- 复现:
  1. 刷新页面并重新进入 Dust2 单人模式。
  2. 进入后前 5 秒观察 HUD phase 区域。
  3. 只看到倒计时，未看到冻结状态标签。
- 建议修复目标: `Cs16BotMatch` 的 freeze phase 显式同步到 HUD；HUD 对 freeze/live 使用不同标签和样式。

### [P1] LIVE 阶段购买菜单会打开但不可操作

- 现象: LIVE 阶段按 `B`，购买菜单打开并显示“只能在冻结购买时间购买”，遮挡战斗 UI。
- 期望: LIVE 阶段按 `B` 不应打开完整购买菜单；应显示短提示并保持战斗视野清晰。
- 复现:
  1. 进入 Dust2 单人模式并等待 LIVE。
  2. 按 `B`。
  3. 观察购买菜单打开但不可购买。
- 建议修复目标: 在非购买期拦截 `toggleBuyMenu`，改为 HUD toast；或者打开只读小提示而非完整菜单。

### [P1] 回合数不随比分更新

- 现象: 第一轮结束后 CT 比分变为 `1`，但 HUD 仍显示 `第 0 回合`；新一轮 timer 已重置到 `01:53`。
- 期望: 第一轮结束后应显示 `第 1 回合` 或下一回合编号，计分板/HUD 保持一致。
- 复现:
  1. 进入 Dust2 单人模式。
  2. 等待一轮自然结束。
  3. 观察 CT 得分后回合编号仍为 `第 0 回合`。
- 建议修复目标: 统一 `round` 的零基/一基展示规则；回合结算时同时更新比分、回合号、HUD 和计分板。

### [P1] 击杀提示为空

- 现象: 观察到 CT 得分从 `0` 到 `1`，但右上角 `击杀提示`区域始终为空。
- 期望: Bot 击杀、玩家击杀或回合胜利至少应有一条短反馈，帮助玩家理解比分变化原因。
- 复现:
  1. 进入 Dust2 单人模式。
  2. 等待 Bot 交战或回合结束。
  3. 观察比分变化但 kill feed 为空。
- 建议修复目标: `Cs16BotMatch` 的 kill/round events 必须进入 `KillFeed`；无击杀的超时结算也应显示回合结果。

### [P2] Tab 计分板触发不稳定

- 现象: 按 `Tab` 后 `.scoreboard` 仍为 `hidden`。这可能与自动化按键只有瞬时 keypress、无法保持按下有关，但从玩家角度仍需要验证。
- 期望: 按住 Tab 时计分板显示，松开隐藏；如果设计为切换，则单按 Tab 应显示。
- 复现:
  1. 进入 LIVE 阶段。
  2. 按 `Tab`。
  3. 观察计分板仍为 hidden。
- 建议修复目标: 增加手动验收或 e2e 按住键测试；确保 keydown/keyup 状态不被 Pointer Lock 或菜单焦点吞掉。

### [P2] 主菜单地图数量在模式切换后显示不一致

- 现象: 初始主菜单显示 `9 张地图`；进入单人后隐藏菜单文本中出现 `8 张地图`，但仍能看到包含 Blood Strike 的地图文本残留。
- 期望: 地图数量应与当前模式过滤后的可见地图一致，隐藏菜单不应残留误导文本。
- 复现:
  1. 打开主菜单，观察地图数量为 `9 张地图`。
  2. 进入单人 Dust2。
  3. 检查隐藏主菜单状态文本，出现 `8 张地图`。
- 建议修复目标: `MainMenu` 模式过滤后同步 count 和 DOM；隐藏状态下避免保留过期可访问文本。

## 复现步骤

1. 在仓库根目录运行 `npm run dev`。
2. 若 Vite 切换端口，使用终端提示的 Local URL，本次为 `http://localhost:5174/`。
3. 在 Chrome 打开页面。
4. 主菜单选择 `Dust2`，点击 `单人任务闯关`。
5. 观察开局前 5 秒 HUD 是否显示冻结状态。
6. LIVE 阶段测试:
   - 按 `B` 打开购买菜单。
   - 按 `2`、`3`、`4` 切换武器。
   - 左键点击画布中心尝试射击。
   - 按 `R` 换弹。
   - 按 `Tab` 查看计分板。
   - 按 `Z` 打开无线电菜单。
7. 等待一轮自然结束，观察比分、回合号和击杀提示。

## 建议验证清单

- [ ] 进入 Dust2 单人模式后显示清晰的冻结购买状态。
- [ ] 冻结期可购买；LIVE 期不会打开遮挡视野的不可操作购买菜单。
- [ ] 鼠标锁定提示可靠显示，锁定成功后左键开火消耗弹药。
- [ ] `2`、`3`、`4` 切换后，当前武器、槽位、弹药和投掷物数量一致。
- [ ] 第一轮结束后回合号、比分、计分板全部同步。
- [ ] Bot 击杀或回合胜利时右上角出现 kill feed/round feed。
- [ ] Tab 计分板在真实键盘按住/松开时表现符合设计。
- [ ] `npm run dev` 在后端端口占用时有清晰提示或自动端口策略。
